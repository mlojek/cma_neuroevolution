{
    "optimization_type": "gradient",
    "optimization_method": "adam",
    "epochs": 50,
    "learning_rate": 0.01,
    "dataset_config": {
        "batch_size": 16,
        "test_size": 0.2
    },
    "logging_config": {
        "use_wandb": false,
        "log_interval": 5
    }
}